<%- include('../partials/header.ejs') %>
<%- include('../partials/navbar.ejs') %>

<div class="container">
    <div class="card mx-5 my-3">
        <div class="card-header ">
            Criar Esquadrão
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <thead>
                  <tr>
                    <th>Id</th>
                    <th>Pelotão</th>
                    <th>Esquadrão</th>
                  </tr>
                </thead>
                <tbody>
                    <% platoons.forEach(platoon => { %>
                    <tr>
                    <td><%= platoon.id %></td>
                    <td><%= platoon.nome %></td>
                    <td><%= platoon.esquad.nome %></td>
                    </tr>
                  <% }); %>
                </tbody>
              </table>
            </form>
        </div>
      </div>
</div>



<%- include('../partials/footer.ejs') %>


    
